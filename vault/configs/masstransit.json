{
  "$schema": "../schemas/config.schema.json",
  "name": "masstransit",
  "description": "MassTransit message bus documentation. Use for distributed messaging, consumers, sagas, state machines, RabbitMQ, Azure Service Bus, Amazon SQS, and event-driven architecture in .NET.",
  "_source": {
    "type": "github",
    "repo": "MassTransit/MassTransit",
    "path": "doc/content"
  },
  "versions": {
    "latest": { "ref": "develop" }
  }
}
