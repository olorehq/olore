{
  "$schema": "../schemas/config.schema.json",
  "name": "rive",
  "description": "Rive animation runtime documentation. Use for Rive React Native integration, Expo setup, animation playback, state machines, data binding, inputs, layouts, events, and asset loading in mobile and web apps.",
  "_source": {
    "type": "github",
    "repo": "rive-app/rive-docs",
    "path": "runtimes"
  },
  "versions": {
    "latest": { "ref": "main" }
  }
}
