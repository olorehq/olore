{
  "$schema": "../schemas/config.schema.json",
  "name": "lucia",
  "description": "Lucia auth documentation. Use for session-based authentication, OAuth 2.0, password hashing, and framework-specific auth implementations.",
  "_source": {
    "type": "github",
    "repo": "lucia-auth/lucia",
    "path": "pages",
    "extensions": [".md"]
  },
  "versions": {
    "latest": { "ref": "main" }
  }
}
