{
  "$schema": "../schemas/config.schema.json",
  "name": "bun",
  "description": "Bun documentation. Use for JavaScript runtime, bundler, package manager, test runner, HTTP server, SQLite, and Node.js compatibility.",
  "_source": {
    "type": "github",
    "repo": "oven-sh/bun",
    "path": "docs",
    "extensions": [".mdx"]
  },
  "versions": {
    "latest": { "ref": "main" }
  }
}
