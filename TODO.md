# olore TODO

## Registry
- [ ] **Fix `latest` version alias for all packages** — packages like `nextjs` only have numbered versions (16.1.1–16.1.5) with no `"latest"` key, so `olore install nextjs` fails. Need a consistent versioning/labeling strategy across all packages. Jason is figuring out how to correctly label these libraries.

## CLI

## Web / Landing Page

## Docs
